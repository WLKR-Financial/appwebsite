import { GasOracleSpeed } from '../../types';
/**
 * @title GasOracleService
 * @author Set Protocol
 *
 * A utility library for fetching current gas prices by speed for Ethereum and Polygon chains
 */
export declare class GasOracleService {
    chainId: number;
    private assert;
    static AVERAGE: GasOracleSpeed;
    static FAST: GasOracleSpeed;
    static FASTEST: GasOracleSpeed;
    constructor(chainId: number);
    /**
     * Returns current gas price estimate for one of 'average', 'fast', 'fastest' speeds.
     * Default speed is 'fast'
     *
     * @param  speed  speed at which tx hopes to be mined / validated by platform
     * @return        gas price to use
     */
    fetchGasPrice(speed?: GasOracleSpeed): Promise<number>;
    private getEthereumGasPrice;
    private getPolygonGasPrice;
}
