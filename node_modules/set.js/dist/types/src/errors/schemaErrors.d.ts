import { ValidatorResult } from '../schemas';
export declare const schemaAssertionsError: {
    DOES_NOT_CONFORM_TO_SCHEMA: (variableName: string, schemaId: string | undefined, value: any, validationResult: ValidatorResult) => string;
};
