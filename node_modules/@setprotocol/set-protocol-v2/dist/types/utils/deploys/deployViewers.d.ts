import { Signer } from "ethers";
import { ProtocolViewer } from "../contracts";
export default class DeployViewers {
    private _deployerSigner;
    constructor(deployerSigner: Signer);
    deployProtocolViewer(): Promise<ProtocolViewer>;
}
