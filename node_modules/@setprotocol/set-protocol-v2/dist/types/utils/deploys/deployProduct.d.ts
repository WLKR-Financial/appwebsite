import { Signer } from "ethers";
import { BigNumber } from "ethers";
import { UniswapYieldHook, AssetLimitHook, AMMSplitter } from "../contracts";
import { Address } from "../types";
export default class DeployProduct {
    private _deployerSigner;
    constructor(deployerSigner: Signer);
    deployUniswapYieldHook(_assets: Address[], _limits: BigNumber[]): Promise<UniswapYieldHook>;
    deployAssetLimitHook(_assets: Address[], _limits: BigNumber[]): Promise<AssetLimitHook>;
    getAssetLimitHook(assetLimitHookAddress: Address): Promise<AssetLimitHook>;
    deployAMMSplitter(uniRouter: Address, sushiRouter: Address, uniFactory: Address, sushiFactory: Address): Promise<AMMSplitter>;
}
