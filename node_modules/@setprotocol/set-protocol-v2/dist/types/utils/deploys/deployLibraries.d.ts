import { Signer } from "ethers";
import { AaveV2, Compound, PerpV2 } from "../contracts";
export default class DeployLibraries {
    private _deployerSigner;
    constructor(deployerSigner: Signer);
    deployCompound(): Promise<Compound>;
    deployAaveV2(): Promise<AaveV2>;
    deployPerpV2(): Promise<PerpV2>;
}
