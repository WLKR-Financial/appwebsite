import { Signer } from "ethers";
import { Address } from "../types";
import { BigNumber } from "ethers";
import { CTokenOracle, YearnVaultOracle } from "../contracts";
export default class DeployOracles {
    private _deployerSigner;
    constructor(deployerSigner: Signer);
    deployCTokenOracle(cToken: Address, underlyingOracle: Address, cTokenFullUnit: BigNumber, underlyingFullUnit: BigNumber, dataDescription: string): Promise<CTokenOracle>;
    deployYearnVaultOracle(vault: Address, underlyingOracle: Address, underlyingFullUnit: BigNumber, dataDescription: string): Promise<YearnVaultOracle>;
}
