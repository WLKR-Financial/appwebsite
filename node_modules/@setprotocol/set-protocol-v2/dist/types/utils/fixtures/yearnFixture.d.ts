import { providers, BigNumber } from "ethers";
import { Vault, Registry } from "../contracts/yearn";
import { Address } from "../types";
export declare class YearnFixture {
    private _deployer;
    private _ownerAddress;
    private _ownerSigner;
    registry: Registry;
    constructor(provider: providers.Web3Provider | providers.JsonRpcProvider, ownerAddress: Address);
    initialize(): Promise<void>;
    createAndEnableVaultWithStrategyMock(underlying: Address, governance: Address, guardian: Address, rewards: Address, name: string, symbol: string, depositLimit: BigNumber): Promise<Vault>;
}
